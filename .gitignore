# Binaries
/songguo
/songguo-*

# SQLite database and WAL/SHM sidecar files
*.db
*.db-*

# Real config (only the example is committed)
/config.yaml

# Frontend build artifacts (backend/web/dist is committed so the binary builds without Node)
frontend/node_modules/
frontend/*.tsbuildinfo
frontend/dist/

# Misc
.DS_Store
*.log
/tmp/
